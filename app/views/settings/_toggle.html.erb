<%# locals: form (a fields_for builder), preference %>
<div class="flex items-center justify-between gap-4 py-3.5">
  <div class="min-w-0">
    <p class="text-sm font-semibold text-gray-900"><%= preference.title %></p>
    <p class="text-xs text-gray-400 mt-0.5"><%= preference.description %></p>
  </div>

  <label class="relative inline-flex items-center cursor-pointer shrink-0">
    <%# check_box emits a hidden "0" before the box, so unchecking actually submits. %>
    <%= form.check_box :enabled, class: "sr-only peer", "aria-label": preference.title %>
    <span class="w-11 h-6 rounded-full bg-gray-200 peer-checked:bg-green-600 transition-colors"></span>
    <span class="absolute left-0.5 w-5 h-5 rounded-full bg-white shadow transition-transform peer-checked:translate-x-5"></span>
  </label>
</div>
