# Name of your application. Used to uniquely configure containers.
service: rails_budget

# Name of the container image (use your-user/app-name on external registries).
image: qjf101/rails_budget

# Deploy to these servers.
servers:
  web:
    hosts:
      - 149.28.57.97

# kamal-proxy runs, but on loopback ports instead of 80/443, because the VPS
# already has nginx bound to those for another app. nginx terminates TLS and
# forwards to kamal-proxy, which owns the port permanently and swaps containers
# behind it — so deploys stay zero-downtime. Publishing the app container directly
# on a fixed port instead means every deploy collides with the running container.
proxy:
  ssl: false                       # nginx terminates TLS, not kamal-proxy
  host: rails-budget.qcservices.io # kamal-proxy routes on the Host header
  run:
    http_port: 3001
    https_port: 3443               # unused with ssl: false, but must not collide
    bind_ips:
      - 127.0.0.1

# Where you keep your container images.
registry:
  server: ghcr.io
  username: qjf101
  # Locally this is a PAT with write:packages; in GitHub Actions the built-in
  # GITHUB_TOKEN works, so CI needs no extra registry secret.
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - RAILS_BUDGET_DATABASE_PASSWORD
    # Re-enable together with the matching lines in .kamal/secrets once you have a
    # mail provider. Kamal rejects an empty secret, so they cannot be left blank.
    # - SMTP_USER_NAME
    # - SMTP_PASSWORD
  clear:
    # See .env.example for what each of these does.
    APP_HOST: rails-budget.qcservices.io
    SMTP_ADDRESS: smtp.example.com
    SMTP_PORT: 587

    # Public demo: every visitor is signed into the demo account and can change its
    # data. A banner says so, and ResetDemoDataJob restores it nightly at 4am.
    DEMO_AUTO_LOGIN: true

    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: true

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # Match this to any external database server to configure Active Record correctly
    # Use rails_budget-db for a db accessory server on same machine via local kamal docker network.
    # DB_HOST: 192.168.0.2

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "rails_budget_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: 3.3.11
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# The same non-root user your other app's GitHub Actions deploys as. It must be in
# the docker group — Kamal only installs Docker when it connects as root.
ssh:
  user: app

# Postgres runs as its own container on the VPS rather than sharing an instance
# with anything else already on the box (secrets come from .kamal/secrets).
accessories:
  db:
    image: postgres:17
    # Same machine as web for now; move to its own host when you outgrow that.
    host: 149.28.57.97
    # Bound to loopback, so Postgres is not exposed to the internet. The app does
    # not use this mapping — it reaches the container over Kamal's private network.
    # It exists so you can `psql -h 127.0.0.1 -p 5433` from the VPS itself.
    port: "127.0.0.1:5433:5432"
    env:
      clear:
        POSTGRES_USER: rails_budget
        # Only creates the primary database. The _cache, _queue and _cable ones are
        # created by `bin/rails db:prepare`, which works because this role is a superuser.
        POSTGRES_DB: rails_budget_production
      secret:
        - POSTGRES_PASSWORD
    volumes:
      # Named volume, so the data survives `kamal accessory reboot db`.
      - "rails_budget_pg_data:/var/lib/postgresql/data"
#   redis:
#     image: valkey/valkey:8
#     host: 192.168.0.2
#     port: 6379
#     directories:
#       - data:/data
